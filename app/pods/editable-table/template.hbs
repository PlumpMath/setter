<thead>
  <tr>
    {{#each h in headings}}
      <th>{{h.label}}</th>
    {{/each}}
  </tr>
</thead>
<tbody>
  {{#each row in rows}}
    {{editable-table-row item=row rowAttrs=rowAttributes}}
  {{/each}}
</tbody>
